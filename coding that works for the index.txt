<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1"/>
  <title>Plane & Simple — Luxury Aircraft Detailing</title>

  <!-- Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Cinzel:wght@400;700&family=Open+Sans:wght@300;400;600;700&display=swap" rel="stylesheet"/>

  <style>
    :root{
      --brand-blue:#001126;
      --brand-gold:#D4AF37;
      --maxw:1200px;
      --t: all .45s ease;
    }
    *{box-sizing:border-box}
    html,body{margin:0;padding:0}
    body{
      font-family:'Open Sans',sans-serif;
      background:#fafafa;color:#111;margin:0;text-align:center;scroll-behavior:smooth
    }
    .container{max-width:var(--maxw);margin:0 auto;padding:0 20px}

    /* Header */
    header{
      position:fixed;top:0;left:0;right:0;z-index:1000;
      background:rgba(0,17,38,.85);backdrop-filter:blur(10px);
      opacity:0;animation:fadeIn .8s ease .2s forwards
    }
    .nav-wrap{display:flex;align-items:center;justify-content:space-between;padding:14px 40px}
    .brandline{
      font-family:'Cinzel',serif;color:var(--brand-gold);font-weight:700;
      font-size:clamp(1.6rem,3vw,2rem);letter-spacing:1.5px;flex:1;text-align:left
    }
    nav{flex:2}
    nav ul{list-style:none;display:flex;justify-content:center;gap:30px;margin:0;padding:0}
    nav a{
      color:#fff;text-decoration:none;font-weight:600;letter-spacing:.6px;position:relative;
      padding-bottom:4px;opacity:0;animation:fadeUp .8s ease .6s forwards
    }
    nav a::after{content:"";position:absolute;left:50%;bottom:0;width:0;height:2px;background:var(--brand-gold);transform:translateX(-50%);transition:var(--t)}
    nav a:hover::after{width:100%}
    .header-cta{
      flex:1;margin-left:auto;background:var(--brand-gold);color:var(--brand-blue);
      padding:10px 18px;border-radius:24px;font-weight:700;font-size:1rem;cursor:pointer;border:none;
      transition:var(--t);text-align:center;opacity:0;animation:fadeUp .8s ease .7s forwards
    }
    .header-cta:hover{background:#b48b25;color:#fff;box-shadow:0 0 10px rgba(212,175,55,.5)}

    /* Hero */
    .hero{
      position:relative;
      height:100vh;
      display:flex;
      align-items:center;
      justify-content:center;
      overflow:hidden;
    }
    .hero img.bg{
      position:absolute;
      top:0;left:0;
      width:100%;height:100%;
      object-fit:cover;
      z-index:0;
    }
    .hero::before{
      content:"";
      position:absolute;
      inset:0;
      background:rgba(0,17,38,.45);
      z-index:1;
    }
    .hero-inner{
      position:relative;
      z-index:2;
      color:#fff;
      display:flex;
      flex-direction:column;
      align-items:center;
      justify-content:center;
    }

    /* Crest */
    .crest{
      width:240px;height:240px;margin:0 auto 24px;position:relative;display:grid;place-items:center;
      opacity:0;transform:translateY(30px);animation:fadeUp 1s ease 1s forwards
    }
    .crest::before{
      content:"";position:absolute;inset:0;border-radius:50%;
      background:radial-gradient(circle, rgba(212,175,55,0.35) 0%, rgba(212,175,55,0) 80%);
      filter:blur(10px);
      z-index:0;animation:pulse 6s ease-in-out infinite
    }
    .crest img{
      width:190px;height:190px;border-radius:50%;position:relative;z-index:2;
      box-shadow:0 0 40px rgba(212,175,55,.6), 0 0 80px rgba(212,175,55,.4)
    }
    .ring{
      position:absolute;inset:0;margin:auto;width:240px;height:240px;border-radius:50%;
      border:4px solid transparent;
      background:conic-gradient(
        from 0deg,
        var(--brand-gold) 0%,
        transparent 25%,
        var(--brand-gold) 50%,
        transparent 75%,
        var(--brand-gold) 100%
      );
      filter:drop-shadow(0 0 12px rgba(212,175,55,.7));
      animation:rotate 18s linear infinite, pulse 7s ease-in-out infinite;
      z-index:1;
    }
    @keyframes rotate{to{transform:rotate(360deg)}}
    @keyframes pulse{
      0%,100%{box-shadow:0 0 30px rgba(212,175,55,.6), 0 0 70px rgba(212,175,55,.35)}
      50%{box-shadow:0 0 48px rgba(212,175,55,.95), 0 0 110px rgba(212,175,55,.55)}
    }

    h1{
      font-family:'Cinzel',serif;color:var(--brand-gold);
      font-size:clamp(2.6rem,6vw,4.6rem);margin:.2rem 0;
      opacity:0;transform:translateY(30px);animation:fadeUp 1s ease 1.2s forwards
    }
    .subtitle{
      color:#fff;letter-spacing:.45rem;font-weight:600;margin:.2rem 0 1rem;text-transform:uppercase;
      opacity:0;transform:translateY(30px);animation:fadeUp 1s ease 1.35s forwards
    }
    .cta{
      background:var(--brand-gold);color:var(--brand-blue);
      padding:14px 34px;border-radius:30px;font-weight:800;text-decoration:none;
      box-shadow:0 8px 22px rgba(212,175,55,.35);transition:var(--t);
      opacity:0;transform:translateY(22px);animation:fadeUp 1s ease 1.5s forwards
    }
    .cta:hover{background:#b48b25;color:#fff;box-shadow:0 0 24px rgba(212,175,55,.6);transform:translateY(0)}

    /* Sections, cards, packages, gallery, footer ... */
    /* (Keep all your original code for About, Services, Packages, Gallery, Airports, Footer, etc.) */
  </style>
</head>
<body>
  <!-- Header -->
  <header>
    <div class="nav-wrap container">
      <div class="brandline">Plane & Simple</div>
      <nav>
        <ul>
          <li><a href="#about">About</a></li>
          <li><a href="#services">Services</a></li>
          <li><a href="#packages">Packages</a></li>
          <li><a href="#gallery">Gallery</a></li>
          <li><a href="#airports">Airports</a></li>
          <li><a href="#contact">Contact</a></li>
        </ul>
      </nav>
      <button class="header-cta" onclick="location.href='#contact'">Book Now</button>
    </div>
  </header>

  <!-- Hero with <img src> -->
  <section class="hero">
    <img src="intro.png" alt="Intro Background" class="bg">
    <div class="hero-inner">
      <div class="crest" aria-label="Illuminated Plane & Simple logo">
        <div class="ring" aria-hidden="true"></div>
        <img src="Logo.png" alt="Plane & Simple Logo">
      </div>
      <h1>Plane &amp; Simple</h1>
      <div class="subtitle">Aircraft Detailing</div>
      <a href="#services" class="cta">Explore Services</a>
    </div>
  </section>

  <!-- About, Services, Packages, Gallery, Airports, Footer -->
  <!-- (Paste back your original sections here, unchanged — only the hero part is modified) -->

</body>
</html>
